\element{distribution}{
  \qp{6}
  \begin{question}{Distribution}
    Quelle est la distribution Linux utilisée à l'IUT par défaut ?
    \begin{rep}[3]
      \mauvaise{Mageia}
      \mauvaise{Red Hat Linux}
      \mauvaise{Fedora}
      \mauvaise{Windows 8.1}
      \bonne{Debian}
      \mauvaise{Ubuntu}
    \end{rep}
  \end{question}
}

\element{fonctionnement}{
  \qp{24}
  \begin{questionmult}{Cycle}
    Quelles sont les quatre étapes qui appartiennent au cycle du processeur ?
    \begin{rep}[2]
      \bonne{Récupération d'instruction}
      \bonne{Décodage d'instruction}
      \bonne{Exécution de l'instruction}
      \bonne{Écriture des résultats}
      \mauvaise{Lecture des résultats}
      \mauvaise{Sortie}
      \mauvaise{Attente de boucle}
      \mauvaise{Synchronisation}
      \mauvaise{Désynchronisation}
    \end{rep}
  \end{questionmult}
}

\element{fonctionnement}{
  \qp{12}
  \begin{questionmult}{Unité de temps}
    
    Quel est l'ordre de grandeur du temps de cycle d'un processeur ? (2 réponses à cocher pour avoir bon, sinon 0)
    \bareme{mz=\questionpoints}
    \begin{rep}[3]
      \bonne{1 nanoseconde}
      \bonne{$10^{-9}$ s}
      \mauvaise{1 milliseconde}
      \mauvaise{$10^{-3}$ s}
      \mauvaise{1 microseconde}
      \mauvaise{$10^{-6}$ s}
      \mauvaise{100 nanosecondes}
      \mauvaise{$10^{-7}$ s}
    \end{rep}
  \end{questionmult}
}

\element{fonctionnement}{
  \qp{12}
  \begin{questionmult}{Le système d'exploitation}
    Quel sont les rôles d'un système d'exploitation ?
    \begin{rep}
      \bonne{Coordonner l'utilisation des ressources matérielles (mémoire, par exemple)}
      \bonne{Permettre un accès facile au contrôle des périphériques pour les programmeurs}
      \bonne{Assurer la maintenance des structures de données du système}
      \mauvaise{Augmenter les capacités physiques de l'ordinateur}
      \mauvaise{Changer l'interface graphique du système en proposant plusieurs aspects}
      \mauvaise{Faire les calculs nécessaires à l'utilisateur, des feuilles de calcul jusqu'aux jeux vidéos}
    \end{rep}
  \end{questionmult}
}

\element{information}{
  \qp{12}
  \begin{questionmult}{Traitement de l'information}
    Quelles sont les affirmations vraies ?
    \begin{rep}
      \bonne{L'information est une quantité mesurable.}\bareme{b=4,m=0}
      \bonne{Un ordinateur manipule des représentations mais n'y attache pas de sens.}
      \mauvaise{Il n'existe qu'une seule façon de représenter un nombre, c'est avec des 0 et des 1.}
      \mauvaise{Un ordinateur manipule des concepts pour calculer des fonctions.}
      \mauvaise{Un ordinateur ne peut manipuler que des 0 et des 1 ; c'est pour ça qu'il ne peut pas écrire un texte grammaticalement correct.}
    \end{rep}
  \end{questionmult}
}